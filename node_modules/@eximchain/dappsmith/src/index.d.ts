// Type definitions for @eximchain/dappsmith

import { MethodAbi } from 'ethereum-types';

export interface Input {
    name: string,
    abi: MethodAbi[]
    contract_addr: string,
    web3URL: string
}

export interface Options {
    build?: boolean,
    start?: boolean
}

export function generate(input:Input, options?:Options): void;