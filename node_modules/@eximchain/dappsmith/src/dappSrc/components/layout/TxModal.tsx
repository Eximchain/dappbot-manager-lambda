import React, { FunctionComponent } from 'react';

interface Props {
    contractName: string
}

export const TxModal: FunctionComponent<Props> = ({contractName}) => {
    // TODO: Determine how this will actually get used in practice.
    return (
        <div />
    )
}

export default TxModal;