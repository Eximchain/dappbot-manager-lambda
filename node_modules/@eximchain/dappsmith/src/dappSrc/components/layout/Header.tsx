import React, { FunctionComponent } from 'react';

interface Props {
    contractName: string
}

export const Header: FunctionComponent<Props> = ({contractName}) => {
    return (
        <h1>{contractName}</h1>
    )
}

export default Header;